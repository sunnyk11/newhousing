import { GetPercentagePipe } from './get-percentage.pipe';

describe('GetPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
