import { GetAveragePipe } from './get-average.pipe';

describe('GetAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new GetAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
