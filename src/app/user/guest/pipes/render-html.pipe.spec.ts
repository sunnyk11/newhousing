import { RenderHtmlPipe } from './render-html.pipe';

describe('RenderHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
