import { AmenitiesactivePipe } from './amenitiesactive.pipe';

describe('AmenitiesactivePipe', () => {
  it('create an instance', () => {
    const pipe = new AmenitiesactivePipe();
    expect(pipe).toBeTruthy();
  });
});
